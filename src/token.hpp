#pragma once

enum TokenType {
    NE, PAS, // ( or {, ) or }
    VIRGULE, PÉRIODE, SOUSTRAIRE, ADDITIONNER, DIVISER, MULTIPLIER, RESTER, // comma, dot, -, +, /, *, %
    TYPE, REFLEXIVE, APPELLER,
    NUMERO,
};
